<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.NoActionBar">
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <!--
        T705 전체화면.
        ★ windowFullscreen 만으로는 내비게이션 바가 남는다. 가로 640x360 게임에서
          하단(회전 시 우측) 제스처 바가 캔버스를 잘라먹으므로 MainActivity 에서
          immersive-sticky 를 추가로 건다. 이 테마는 "상태바 없음 + 컷아웃까지 그림"만 담당한다.
    -->
    <style name="AppTheme.NoActionBar" parent="Theme.AppCompat.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:windowFullscreen">true</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:windowBackground">@color/splashBackground</item>
        <!-- 노치 뒤까지 캔버스를 채운다. index.html 의 viewport-fit=cover 와 짝이다. -->
        <item name="android:windowLayoutInDisplayCutoutMode">shortEdges</item>
    </style>

    <!-- 콜드 스타트 동안 보이는 테마. 흰 플래시를 막으려면 배경이 반드시 어두워야 한다. -->
    <style name="AppTheme.NoActionBarLaunch" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/splashBackground</item>
        <item name="postSplashScreenTheme">@style/AppTheme.NoActionBar</item>
        <item name="android:background">@drawable/splash</item>
    </style>
</resources>
